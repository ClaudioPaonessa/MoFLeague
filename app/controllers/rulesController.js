var URL = "https://mof-league.com"

if (window.location.hostname == 'localhost') {
    API_URL = "";
}

app.controller("RulesController", function($scope, $http, $window) {
    
    $scope.initRules = function() {
        
    }

    $scope.initRules();
});
