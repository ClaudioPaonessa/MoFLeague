function copyToClipboard(text) {
  window.prompt("Copy to clipboard: Ctrl+C, Enter", text);
}

copyToClipboard(Array.prototype.slice.call(document.getElementById('booster-container').querySelectorAll("img")).map(a => a.currentSrc).join(';'));
